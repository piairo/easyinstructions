<%= render 'shared/navbar_wagon' %>

<div class="banner mb-3">  <!-- HOME(s)FOR BOOKING Guest-->

      <h3 style="display: inline" id="typed" data-strings="<%= ["step by step"].to_json  %>" data-speed="150" data-loop="true"></h3>

  <br>

  <div class="row justify-content-center mb-3">
    <%= link_to step_path(@field, @instruction.id,1) do %>
    <%= image_tag "logo_easyinstructions6_large.png", id:"home-img"%>
    <% end %>
  </div>


  <div class="container d-flex align-items-center justify-content-center" style='height: 20vh'>

      <%= link_to "Check-in", step_path(@field, @instruction.id,1), class: 'btn btn-danger mybutton-lg home', id: 'blinkbutton' %>
  </div>

  <div class="container d-flex align-items-center justify-content-center">

       <% if @keyinfo.empty? %>

       <% else %>
        <%= link_to "house manual", field_instruction_keyinfo_path(@field, @instruction,1), class: 'btn btn-secondary mybutton-sm home'%>
       <% end %>

  </div>

</div>

<%= render 'shared/navbar_bottom' %>

