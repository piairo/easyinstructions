
<div class="navbar_bottom fixed-bottom">

  <!--   BUTTON BACK  --->
    <div>
<!--        1) for the keyinfo page -->
        <% if params[:controller] == 'keyinfos' %>
          <%= link_to 'Back', 'javascript:history.back()', class: 'btn mybutton-light mybutton-lg'%>
<!--        2) for the home page    -->
        <% elsif @arrival.nil? %>

        <% else %>
          <%= link_to 'Back', 'javascript:history.back()', class: 'btn mybutton-light mybutton-lg'%>
        <% end %>
    </div>



<%# RESPONSIVE MENU BUTTON%>
  <div style="text-align: center">
    <button class="btn navbar-toggler p-0" data-toggle="canvas" data-target="#bs-canvas-right" aria-expanded="false" aria-controls="bs-canvas-right" style="color: white"><h1 class="fas fa-bars m-0" style="font-size: 2rem"></h1></button>

<%# KEY INFO BUTTON%>
          <%# if @flat.nil? %>

          <%# elsif params[:controller] == 'keyinfos' %>

          <%# else %>
            <%#= link_to flat_keyinfo_path(@flat, 1) do %>
<!--             <i class="fas fa-info text-light" style="font-size: 1.5rem"></i> -->
            <%# end %>
          <%# end %>
  </div>


 <!--   BUTTON NEXT  --->
   <div>

<!--        1) for the keyinfo page -->
          <%# if params[:controller] == 'keyinfos' %>
            <%#= link_to "home", homes_for_booking_path(@flat.name), class: 'btn btn-primary mybutton-lg'%>
<!--        2) for the home page    -->
          <% if current_page?('/chocolate') || current_page?('/netflix') || current_page?('/hotwater') || current_page?('/pancakes') || current_page?('/frontdoor') || current_page?('/pooridge') || current_page?('/')%>


          <% elsif params[:controller] == 'keyinfos'%>

          <%= link_to "home", homes_for_booking_path("chocolate"), class: 'btn btn-primary mybutton-lg'%>


<!--          SEND SMS FOR CHOCOLATE -->
          <% elsif @flat.name == "chocolate" && @arrival.position == 5 %>
          <%= link_to "next", sms_path(@flat), class: 'btn btn-danger mybutton-lg'%>


          <% elsif @arrival.position == @flat.arrivals.count && ["netflix", "hotwater", "frontdoor", "chocolate"].include?(@flat.name) %>
           <%= link_to "house manual", flat_keyinfo_path(1, 1), class: 'btn btn-danger mybutton-lg auto'%>


          <% elsif @arrival.position == @flat.arrivals.count %>
          <%= link_to "home", root_path, class: 'btn btn-primary mybutton-lg'%>

          <% else %>
            <%= link_to "next", flat_arrival_path(@flat, @arrival.position.to_i + 1), class: 'btn btn-danger mybutton-lg' %>
          <% end %>
    </div>

    </div>
